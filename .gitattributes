# BED test fixtures are byte-exact: line endings, trailing whitespace, and
# encoding edges are part of what the validator asserts against. Treat as
# binary so git never normalizes them (core.autocrlf, eol, etc.).
tests/data/bed/**/*.bed binary
tests/corpus/bed/**/*.bed binary
